package geom

import (
	"testing"
	"time"
)

func TestIndex(t *testing.T) {

	ring := NewRing(tx, false)
	var rects []Rect
	ring.Scan(func(seg Segment) bool {
		rects = append(rects, seg.Rect())
		return true
	})

	// for i := 0; i <= len(segs); i++ {
	// 	start := time.Now()
	// 	tr := newRTree(segs[:i])
	// 	println(i, len(segs), time.Since(start).String())
	// 	_ = tr
	// }
	start := time.Now()
	tr := newRTree(rects)
	println(time.Since(start).String())
	_ = tr

	//println(tr)
	//println(tr.svg())
}
